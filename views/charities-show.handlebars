<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <h1>{{charityName}}</h1>
        <p>{{tagLine}}</p>

        {{#each charity.donations}}
            {{> donations}}
        {{/each}}

        <hr> {{> comment-form}}

        <!-- Show Comments -->
        {{#each comments}}
            {{> comment}}
        {{/each}}

    </div>
</div>
