<div class="card">
    <div class="card-block">
        <h2 class="card-title"><a href="/charities/{{this.id}}">{{this.name}}</a></h2>
        <div class="card-body">
            <p class="card-text">
                {{this}}
            </p>
        </div>
    </div>
</div>
